/**
 * Weather Skill
 *
 * Get current weather conditions and forecasts for any location worldwide.
 * Uses the free Open-Meteo API (no API key required).
 */

export default {
  async install() {},
  async activate() {
    console.log('[weather] Activated')
  },
  async deactivate() {},
  async uninstall() {},
}
