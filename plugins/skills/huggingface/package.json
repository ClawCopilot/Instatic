{
  "name": "@instatic/huggingface",
  "version": "0.1.0",
  "description": "HuggingFace Hub integration — search models, datasets, Spaces, and run inference.",
  "instaticManifest": {
    "id": "instatic.huggingface",
    "name": "HuggingFace",
    "version": "0.1.0",
    "apiVersion": 1,
    "description": "Seamlessly interact with HuggingFace Hub: search models, datasets, and Spaces, get model details, and run inference on 800K+ ML models.",
    "permissions": [
      "network.outbound",
      "cms.routes.public"
    ],
    "networkAllowedHosts": [
      "huggingface.co",
      "api-inference.huggingface.co"
    ],
    "entrypoints": {
      "server": "dist/index.js"
    },
    "resources": [],
    "adminPages": [],
    "settings": [
      {
        "id": "apiToken",
        "label": "HuggingFace API Token",
        "description": "Access token from https://huggingface.co/settings/tokens. Required for inference; optional for public search.",
        "type": "password",
        "required": false,
        "secret": true
      }
    ],
    "kind": "skill",
    "aiTools": [
      {
        "name": "search_models",
        "description": "Search the HuggingFace Hub for AI/ML models by keyword, task type, author, or sort by popularity (downloads, likes, trending). Returns model ID, downloads, likes, tags, and pipeline task.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "query": {
              "type": "string",
              "description": "Search query (e.g. 'bert', 'stable diffusion', 'llama')"
            },
            "task": {
              "type": "string",
              "description": "Filter by pipeline task: text-generation, text-classification, image-classification, image-generation, summarization, translation, question-answering, token-classification, speech-recognition, text-to-speech, fill-mask, etc.",
              "enum": [
                "text-generation",
                "text-classification",
                "token-classification",
                "question-answering",
                "fill-mask",
                "summarization",
                "translation",
                "image-classification",
                "image-generation",
                "image-to-text",
                "object-detection",
                "speech-recognition",
                "text-to-speech",
                "text-to-image",
                "zero-shot-classification",
                "sentence-similarity",
                "feature-extraction"
              ]
            },
            "author": {
              "type": "string",
              "description": "Filter by author/organization (e.g. 'meta-llama', 'google', 'mistralai')"
            },
            "sort": {
              "type": "string",
              "description": "Sort order",
              "enum": ["downloads", "likes", "trending", "modified", "created"],
              "default": "trending"
            },
            "limit": {
              "type": "integer",
              "description": "Max results (1-30)",
              "minimum": 1,
              "maximum": 30,
              "default": 10
            }
          }
        }
      },
      {
        "name": "get_model_info",
        "description": "Get detailed information about a specific HuggingFace model: description, tags, pipeline type, downloads, likes, config, library, and a snippet of the model card (README).",
        "inputSchema": {
          "type": "object",
          "properties": {
            "model_id": {
              "type": "string",
              "description": "Model ID on HuggingFace Hub (e.g. 'meta-llama/Llama-3.1-8B', 'bert-base-uncased')"
            }
          },
          "required": ["model_id"]
        }
      },
      {
        "name": "search_datasets",
        "description": "Search the HuggingFace Hub for datasets by keyword, task, or author. Returns dataset ID, downloads, likes, and tags.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "query": {
              "type": "string",
              "description": "Search query (e.g. 'imdb', 'cifar', 'wikipedia')"
            },
            "author": {
              "type": "string",
              "description": "Filter by author/organization"
            },
            "sort": {
              "type": "string",
              "description": "Sort order",
              "enum": ["downloads", "likes", "trending", "modified", "created"],
              "default": "trending"
            },
            "limit": {
              "type": "integer",
              "description": "Max results (1-30)",
              "minimum": 1,
              "maximum": 30,
              "default": 10
            }
          }
        }
      },
      {
        "name": "search_spaces",
        "description": "Search the HuggingFace Hub for Spaces (interactive ML app demos) by keyword or author. Returns Space ID, author, SDK, likes, and status.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "query": {
              "type": "string",
              "description": "Search query (e.g. 'stable diffusion', 'chatbot', 'whisper')"
            },
            "author": {
              "type": "string",
              "description": "Filter by author/organization"
            },
            "sort": {
              "type": "string",
              "description": "Sort order",
              "enum": ["likes", "trending", "modified", "created"],
              "default": "trending"
            },
            "limit": {
              "type": "integer",
              "description": "Max results (1-30)",
              "minimum": 1,
              "maximum": 30,
              "default": 10
            }
          }
        }
      },
      {
        "name": "run_inference",
        "description": "Run inference on a HuggingFace model using the Serverless Inference API. Supports text generation, text classification, image classification, summarization, question answering, NER, translation, and more. Requires an API token (set in plugin settings or HUGGINGFACE_API_TOKEN env var) for most models.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "model_id": {
              "type": "string",
              "description": "Model ID (e.g. 'meta-llama/Meta-Llama-3-8B-Instruct', 'distilbert-base-uncased-finetuned-sst-2-english')"
            },
            "inputs": {
              "type": "string",
              "description": "Input text for the model (prompt, sentence, question, etc.)"
            },
            "parameters": {
              "type": "object",
              "description": "Optional model-specific parameters (e.g. {max_new_tokens: 100, temperature: 0.7, top_p: 0.9} for text generation)",
              "properties": {
                "max_new_tokens": { "type": "integer", "description": "Max tokens to generate (text generation)" },
                "temperature": { "type": "number", "description": "Sampling temperature (0-2)" },
                "top_p": { "type": "number", "description": "Top-p sampling (0-1)" },
                "top_k": { "type": "integer", "description": "Top-k sampling" },
                "return_full_text": { "type": "boolean", "description": "Return prompt + generated text" }
              }
            }
          },
          "required": ["model_id", "inputs"]
        }
      },
      {
        "name": "get_dataset_info",
        "description": "Get detailed information about a specific HuggingFace dataset: description, tags, download stats, file tree listing, and README content. Use this to inspect a dataset before downloading or to understand its structure.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "dataset_id": {
              "type": "string",
              "description": "Dataset ID on HuggingFace Hub (e.g. 'imdb', 'HuggingFaceFW/fineweb', 'wikitext')"
            },
            "with_files": {
              "type": "boolean",
              "description": "Include file tree listing",
              "default": true
            }
          },
          "required": ["dataset_id"]
        }
      },
      {
        "name": "list_repo_files",
        "description": "List files and directories in a HuggingFace repository (model, dataset, or Space). Returns paths, types (file/directory), sizes, and LFS status. Useful for exploring repo contents before downloading or backing up.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "repo_id": {
              "type": "string",
              "description": "Repository ID (e.g. 'meta-llama/Llama-3.1-8B', 'imdb', 'stabilityai/stable-diffusion-xl-base-1.0')"
            },
            "repo_type": {
              "type": "string",
              "description": "Repository type",
              "enum": ["model", "dataset", "space"],
              "default": "model"
            },
            "path": {
              "type": "string",
              "description": "Subdirectory path to list (e.g. 'data', 'configs', 'src'). Leave empty for root.",
              "default": ""
            }
          },
          "required": ["repo_id"]
        }
      },
      {
        "name": "create_repo",
        "description": "Create a new repository on HuggingFace Hub: model, dataset, or Space. Requires a write-enabled API token (set in plugin settings or HUGGINGFACE_API_TOKEN env var). For Spaces, specify the SDK (gradio, streamlit, or static).",
        "inputSchema": {
          "type": "object",
          "properties": {
            "name": {
              "type": "string",
              "description": "Repository name. For personal repos: 'my-repo'. For organization repos: 'org-name/repo-name'."
            },
            "repo_type": {
              "type": "string",
              "description": "Repository type to create",
              "enum": ["model", "dataset", "space"],
              "default": "model"
            },
            "private": {
              "type": "boolean",
              "description": "Create as private repository",
              "default": false
            },
            "sdk": {
              "type": "string",
              "description": "For Spaces only: UI framework. Options: gradio, streamlit, static.",
              "enum": ["gradio", "streamlit", "static"]
            }
          },
          "required": ["name"]
        }
      },
      {
        "name": "upload_file",
        "description": "Upload a text file to a HuggingFace repository (model, dataset, or Space). Requires a write-enabled API token. Useful for uploading configs, README updates, code files, or small dataset files. For large binary files, use the HuggingFace CLI or Git LFS instead.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "repo_id": {
              "type": "string",
              "description": "Target repository ID (e.g. 'username/my-dataset')"
            },
            "repo_type": {
              "type": "string",
              "description": "Repository type",
              "enum": ["model", "dataset", "space"],
              "default": "model"
            },
            "path_in_repo": {
              "type": "string",
              "description": "Target file path inside the repository (e.g. 'README.md', 'data/train.json', 'app.py')"
            },
            "content": {
              "type": "string",
              "description": "Text content to upload"
            },
            "message": {
              "type": "string",
              "description": "Git commit message",
              "default": "Upload file via Instatic"
            }
          },
          "required": ["repo_id", "path_in_repo", "content"]
        }
      },
      {
        "name": "get_repo_readme",
        "description": "Fetch the README.md content of a HuggingFace repository (model, dataset, or Space). Returns the raw markdown text, useful for understanding what a repo does without browsing the website.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "repo_id": {
              "type": "string",
              "description": "Repository ID (e.g. 'meta-llama/Llama-3.1-8B', 'imdb')"
            },
            "repo_type": {
              "type": "string",
              "description": "Repository type",
              "enum": ["model", "dataset", "space"],
              "default": "model"
            }
          },
          "required": ["repo_id"]
        }
      }
    ],
    "systemPrompt": "You are a HuggingFace Hub integration assistant. You can search for AI/ML models, datasets, and Spaces, inspect repository contents, create new repositories, upload files, and run inference on models.\n\n## Capabilities\n- **search_models**: Find models by keyword, task type, author, or sort by popularity. Use this when users want to discover models for a task.\n- **get_model_info**: Get detailed info about a specific model (README snippet, tags, downloads, config). Use this after search to deep-dive.\n- **search_datasets**: Find datasets for training or evaluation. Sort by 'created' to find the newest datasets.\n- **get_dataset_info**: Get full dataset details including file tree and README. Use this to inspect a dataset's structure before downloading.\n- **search_spaces**: Find interactive ML app demos on HuggingFace Spaces.\n- **list_repo_files**: List files in any repository (model/dataset/space). Use this to explore contents, check file sizes, or find specific files.\n- **get_repo_readme**: Fetch the README markdown of any repository.\n- **run_inference**: Run a model on input text. Requires an API token.\n- **create_repo**: Create a new model, dataset, or Space repository. Requires a write-enabled API token.\n- **upload_file**: Upload a text file to a repository. Requires a write-enabled API token.\n\n## Guidelines\n- When a user asks about a task (e.g. 'I need a sentiment analysis model'), use search_models with the appropriate task filter first.\n- To find the latest datasets, use search_datasets with sort='created'.\n- To back up or inspect a dataset, first use get_dataset_info to see its structure, then use list_repo_files to explore subdirectories. For downloading, provide the user with direct links: https://huggingface.co/datasets/{dataset_id}/resolve/main/{file_path}\n- For creating Spaces: set repo_type='space' and choose sdk='gradio' (default UI), 'streamlit', or 'static'.\n- Always present model/dataset results in a clear format: ID, type, downloads, likes, and brief description.\n- For inference and write operations, ensure the user has set their HuggingFace API token in plugin settings. If no token is configured, inform them how to get one at https://huggingface.co/settings/tokens.\n- Popular model IDs for reference: meta-llama/Llama-3.1-8B-Instruct, mistralai/Mistral-7B-Instruct, bert-base-uncased, distilbert-base-uncased-finetuned-sst-2-english, google/flan-t5-large, sentence-transformers/all-MiniLM-L6-v2.\n- Respond in the language the user writes in."
  }
}
