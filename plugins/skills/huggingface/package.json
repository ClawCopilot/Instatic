{
  "name": "@instatic/huggingface",
  "version": "0.1.0",
  "description": "HuggingFace Hub full integration — search models, datasets, Spaces, run inference, and manage resources via hf CLI (Space, Endpoints, Cache, Transfer, Tags).",
  "instaticManifest": {
    "id": "instatic.huggingface",
    "name": "HuggingFace",
    "version": "0.1.0",
    "apiVersion": 1,
    "description": "Seamlessly interact with HuggingFace Hub: search models, datasets, and Spaces, get model details, run inference, manage Spaces and Endpoints, transfer repos, manage tags and cache — all through REST API and hf CLI integration.",
    "permissions": [
      "network.outbound",
      "cms.routes.public"
    ],
    "networkAllowedHosts": [
      "huggingface.co",
      "api-inference.huggingface.co"
    ],
    "entrypoints": {
      "server": "dist/index.js"
    },
    "resources": [],
    "adminPages": [],
    "settings": [
      {
        "id": "apiToken",
        "label": "HuggingFace API Token",
        "description": "Access token from https://huggingface.co/settings/tokens. Required for inference; optional for public search.",
        "type": "password",
        "required": false,
        "secret": true
      }
    ],
    "kind": "skill",
    "aiTools": [
      {
        "name": "search_models",
        "description": "Search the HuggingFace Hub for AI/ML models by keyword, task type, author, tags, language, or sort by popularity (downloads, likes, trending). Returns model ID, downloads, likes, tags, and pipeline task. Supports advanced filtering by multiple tags and programming language.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "query": {
              "type": "string",
              "description": "Search query (e.g. 'bert', 'stable diffusion', 'llama')"
            },
            "task": {
              "type": "string",
              "description": "Filter by pipeline task: text-generation, text-classification, image-classification, image-generation, summarization, translation, question-answering, token-classification, speech-recognition, text-to-speech, fill-mask, etc.",
              "enum": [
                "text-generation",
                "text-classification",
                "token-classification",
                "question-answering",
                "fill-mask",
                "summarization",
                "translation",
                "image-classification",
                "image-generation",
                "image-to-text",
                "object-detection",
                "speech-recognition",
                "text-to-speech",
                "text-to-image",
                "zero-shot-classification",
                "sentence-similarity",
                "feature-extraction"
              ]
            },
            "author": {
              "type": "string",
              "description": "Filter by author/organization (e.g. 'meta-llama', 'google', 'mistralai')"
            },
            "library": {
              "type": "string",
              "description": "Filter by ML library: transformers, diffusers, timm, sentence-transformers, onnx, keras, safetensors, etc."
            },
            "tags": {
              "type": "array",
              "items": { "type": "string" },
              "description": "Filter by tags (e.g. ['pytorch', 'safetensors', 'llama']). Models must have ALL specified tags.",
              "default": []
            },
            "language": {
              "type": "string",
              "description": "Filter by language code (e.g. 'en', 'zh', 'fr', 'multilingual')"
            },
            "sort": {
              "type": "string",
              "description": "Sort order",
              "enum": ["downloads", "likes", "trending", "modified", "created"],
              "default": "trending"
            },
            "limit": {
              "type": "integer",
              "description": "Max results (1-30)",
              "minimum": 1,
              "maximum": 30,
              "default": 10
            },
            "full": {
              "type": "boolean",
              "description": "Return full model details including config, siblings, and card metadata. Slower but more complete.",
              "default": false
            }
          }
        }
      },
      {
        "name": "get_model_info",
        "description": "Get comprehensive information about a specific HuggingFace model: description, tags, pipeline type, downloads, likes, config, library, siblings (file list), and README snippet. Use this to deep-dive into a model's structure and capabilities.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "model_id": {
              "type": "string",
              "description": "Model ID on HuggingFace Hub (e.g. 'meta-llama/Llama-3.1-8B', 'bert-base-uncased')"
            },
            "with_files": {
              "type": "boolean",
              "description": "Include sibling files list (config.json, model weights, tokenizer files, etc.)",
              "default": true
            }
          },
          "required": ["model_id"]
        }
      },
      {
        "name": "search_datasets",
        "description": "Search the HuggingFace Hub for datasets by keyword, task, author, tags, language, or size category. Returns dataset ID, downloads, likes, and tags.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "query": {
              "type": "string",
              "description": "Search query (e.g. 'imdb', 'cifar', 'wikipedia')"
            },
            "task": {
              "type": "string",
              "description": "Filter by task type: text-classification, token-classification, question-answering, text-generation, summarization, translation, image-classification, object-detection, speech-recognition, etc."
            },
            "author": {
              "type": "string",
              "description": "Filter by author/organization"
            },
            "tags": {
              "type": "array",
              "items": { "type": "string" },
              "description": "Filter by tags (e.g. ['text', 'english', 'classification']). Datasets must have ALL specified tags.",
              "default": []
            },
            "language": {
              "type": "string",
              "description": "Filter by language code (e.g. 'en', 'zh', 'fr', 'multilingual')"
            },
            "size_category": {
              "type": "string",
              "description": "Filter by dataset size: n<1K, 1K<n<10K, 10K<n<100K, 100K<n<1M, 1M<n<10M, 10M<n<100M, 100M<n<1B, n>1B",
              "enum": ["n<1K", "1K<n<10K", "10K<n<100K", "100K<n<1M", "1M<n<10M", "10M<n<100M", "100M<n<1B", "n>1B"]
            },
            "sort": {
              "type": "string",
              "description": "Sort order",
              "enum": ["downloads", "likes", "trending", "modified", "created"],
              "default": "trending"
            },
            "limit": {
              "type": "integer",
              "description": "Max results (1-30)",
              "minimum": 1,
              "maximum": 30,
              "default": 10
            }
          }
        }
      },
      {
        "name": "search_spaces",
        "description": "Search the HuggingFace Hub for Spaces (interactive ML app demos) by keyword or author. Returns Space ID, author, SDK, likes, and status.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "query": {
              "type": "string",
              "description": "Search query (e.g. 'stable diffusion', 'chatbot', 'whisper')"
            },
            "author": {
              "type": "string",
              "description": "Filter by author/organization"
            },
            "sort": {
              "type": "string",
              "description": "Sort order",
              "enum": ["likes", "trending", "modified", "created"],
              "default": "trending"
            },
            "limit": {
              "type": "integer",
              "description": "Max results (1-30)",
              "minimum": 1,
              "maximum": 30,
              "default": 10
            }
          }
        }
      },
      {
        "name": "run_inference",
        "description": "Run inference on a HuggingFace model using the Serverless Inference API. Supports text generation, text classification, image classification, summarization, question answering, NER, translation, and more. Requires an API token (set in plugin settings or HUGGINGFACE_API_TOKEN env var) for most models.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "model_id": {
              "type": "string",
              "description": "Model ID (e.g. 'meta-llama/Meta-Llama-3-8B-Instruct', 'distilbert-base-uncased-finetuned-sst-2-english')"
            },
            "inputs": {
              "type": "string",
              "description": "Input text for the model (prompt, sentence, question, etc.)"
            },
            "parameters": {
              "type": "object",
              "description": "Optional model-specific parameters (e.g. {max_new_tokens: 100, temperature: 0.7, top_p: 0.9} for text generation)",
              "properties": {
                "max_new_tokens": { "type": "integer", "description": "Max tokens to generate (text generation)" },
                "temperature": { "type": "number", "description": "Sampling temperature (0-2)" },
                "top_p": { "type": "number", "description": "Top-p sampling (0-1)" },
                "top_k": { "type": "integer", "description": "Top-k sampling" },
                "return_full_text": { "type": "boolean", "description": "Return prompt + generated text" }
              }
            }
          },
          "required": ["model_id", "inputs"]
        }
      },
      {
        "name": "get_dataset_info",
        "description": "Get detailed information about a specific HuggingFace dataset: description, tags, download stats, file tree listing, and README content. Use this to inspect a dataset before downloading or to understand its structure.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "dataset_id": {
              "type": "string",
              "description": "Dataset ID on HuggingFace Hub (e.g. 'imdb', 'HuggingFaceFW/fineweb', 'wikitext')"
            },
            "with_files": {
              "type": "boolean",
              "description": "Include file tree listing",
              "default": true
            }
          },
          "required": ["dataset_id"]
        }
      },
      {
        "name": "list_repo_files",
        "description": "List files and directories in a HuggingFace repository (model, dataset, or Space). Returns paths, types (file/directory), sizes, and LFS status. Useful for exploring repo contents before downloading or backing up.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "repo_id": {
              "type": "string",
              "description": "Repository ID (e.g. 'meta-llama/Llama-3.1-8B', 'imdb', 'stabilityai/stable-diffusion-xl-base-1.0')"
            },
            "repo_type": {
              "type": "string",
              "description": "Repository type",
              "enum": ["model", "dataset", "space"],
              "default": "model"
            },
            "path": {
              "type": "string",
              "description": "Subdirectory path to list (e.g. 'data', 'configs', 'src'). Leave empty for root.",
              "default": ""
            }
          },
          "required": ["repo_id"]
        }
      },
      {
        "name": "create_repo",
        "description": "Create a new repository on HuggingFace Hub: model, dataset, or Space. Requires a write-enabled API token (set in plugin settings or HUGGINGFACE_API_TOKEN env var). For Spaces, specify the SDK (gradio, streamlit, or static).",
        "inputSchema": {
          "type": "object",
          "properties": {
            "name": {
              "type": "string",
              "description": "Repository name. For personal repos: 'my-repo'. For organization repos: 'org-name/repo-name'."
            },
            "repo_type": {
              "type": "string",
              "description": "Repository type to create",
              "enum": ["model", "dataset", "space"],
              "default": "model"
            },
            "private": {
              "type": "boolean",
              "description": "Create as private repository",
              "default": false
            },
            "sdk": {
              "type": "string",
              "description": "For Spaces only: UI framework. Options: gradio, streamlit, static.",
              "enum": ["gradio", "streamlit", "static"]
            },
            "description": {
              "type": "string",
              "description": "Repository description / README content (used as initial README.md)."
            },
            "license": {
              "type": "string",
              "description": "License identifier (e.g. 'mit', 'apache-2.0', 'cc-by-sa-4.0'). See https://huggingface.co/docs/hub/repositories-licenses"
            }
          },
          "required": ["name"]
        }
      },
      {
        "name": "upload_file",
        "description": "Upload a text file to a HuggingFace repository (model, dataset, or Space). Requires a write-enabled API token. Useful for uploading configs, README updates, code files, or small dataset files. For large binary files, use the HuggingFace CLI or Git LFS instead.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "repo_id": {
              "type": "string",
              "description": "Target repository ID (e.g. 'username/my-dataset')"
            },
            "repo_type": {
              "type": "string",
              "description": "Repository type",
              "enum": ["model", "dataset", "space"],
              "default": "model"
            },
            "path_in_repo": {
              "type": "string",
              "description": "Target file path inside the repository (e.g. 'README.md', 'data/train.json', 'app.py')"
            },
            "content": {
              "type": "string",
              "description": "Text content to upload"
            },
            "message": {
              "type": "string",
              "description": "Git commit message",
              "default": "Upload file via Instatic"
            }
          },
          "required": ["repo_id", "path_in_repo", "content"]
        }
      },
      {
        "name": "get_repo_readme",
        "description": "Fetch the README.md content of a HuggingFace repository (model, dataset, or Space). Returns the raw markdown text, useful for understanding what a repo does without browsing the website.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "repo_id": {
              "type": "string",
              "description": "Repository ID (e.g. 'meta-llama/Llama-3.1-8B', 'imdb')"
            },
            "repo_type": {
              "type": "string",
              "description": "Repository type",
              "enum": ["model", "dataset", "space"],
              "default": "model"
            }
          },
          "required": ["repo_id"]
        }
      },
      {
        "name": "get_whoami",
        "description": "Get the current authenticated HuggingFace user information: username, name, email, organizations, and token permissions. Useful for verifying that the API token is configured correctly.",
        "inputSchema": {
          "type": "object",
          "properties": {}
        }
      },
      {
        "name": "download_file",
        "description": "Get a direct download link or raw content of a file from a HuggingFace repository (model, dataset, or Space). Supports downloading text files, config files, small dataset slices, etc. For large files or folders, use run_hf_command with 'hf download' instead.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "repo_id": {
              "type": "string",
              "description": "Repository ID (e.g. 'meta-llama/Llama-3.1-8B')"
            },
            "repo_type": {
              "type": "string",
              "description": "Repository type",
              "enum": ["model", "dataset", "space"],
              "default": "model"
            },
            "path": {
              "type": "string",
              "description": "File path inside the repository (e.g. 'config.json', 'data/train-00000-of-00001.parquet', 'README.md')"
            },
            "revision": {
              "type": "string",
              "description": "Git revision: branch name, tag, or commit hash. Defaults to 'main'.",
              "default": "main"
            },
            "return_content": {
              "type": "boolean",
              "description": "If true and file is text-based, return the file content. Otherwise return the download URL only.",
              "default": false
            }
          },
          "required": ["repo_id", "path"]
        }
      },
      {
        "name": "delete_repo",
        "description": "Delete a HuggingFace repository (model, dataset, or Space). This action is irreversible. Requires a write-enabled API token with admin permissions.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "repo_id": {
              "type": "string",
              "description": "Repository ID to delete (e.g. 'username/my-repo')"
            },
            "repo_type": {
              "type": "string",
              "description": "Repository type",
              "enum": ["model", "dataset", "space"],
              "default": "model"
            }
          },
          "required": ["repo_id"]
        }
      },
      {
        "name": "delete_file",
        "description": "Delete a file from a HuggingFace repository (model, dataset, or Space). Requires a write-enabled API token. This action creates a new commit removing the file.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "repo_id": {
              "type": "string",
              "description": "Repository ID (e.g. 'username/my-dataset')"
            },
            "repo_type": {
              "type": "string",
              "description": "Repository type",
              "enum": ["model", "dataset", "space"],
              "default": "model"
            },
            "path_in_repo": {
              "type": "string",
              "description": "File path inside the repository to delete (e.g. 'data/old.json', 'README.md')"
            },
            "message": {
              "type": "string",
              "description": "Git commit message",
              "default": "Delete file via Instatic"
            }
          },
          "required": ["repo_id", "path_in_repo"]
        }
      },
      {
        "name": "get_trending",
        "description": "Get trending models, datasets, or Spaces from HuggingFace Hub. Returns the most popular items in the last 24 hours or 7 days.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "description": "Type of item to get trending",
              "enum": ["model", "dataset", "space"],
              "default": "model"
            },
            "period": {
              "type": "string",
              "description": "Trending period",
              "enum": ["day", "week"],
              "default": "day"
            },
            "limit": {
              "type": "integer",
              "description": "Max results (1-30)",
              "minimum": 1,
              "maximum": 30,
              "default": 10
            }
          }
        }
      },
      {
        "name": "list_collections",
        "description": "Search HuggingFace Collections (curated lists of models, datasets, and Spaces). Collections are user-curated groupings of related resources.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "query": {
              "type": "string",
              "description": "Search query (e.g. 'llama', 'diffusion', 'nlp')"
            },
            "author": {
              "type": "string",
              "description": "Filter by collection owner"
            },
            "limit": {
              "type": "integer",
              "description": "Max results (1-30)",
              "minimum": 1,
              "maximum": 30,
              "default": 10
            }
          }
        }
      },
      {
        "name": "list_repo_refs",
        "description": "List branches, tags, and commits in a HuggingFace repository. Useful for finding specific versions or tracking repository history.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "repo_id": {
              "type": "string",
              "description": "Repository ID (e.g. 'meta-llama/Llama-3.1-8B')"
            },
            "repo_type": {
              "type": "string",
              "description": "Repository type",
              "enum": ["model", "dataset", "space"],
              "default": "model"
            },
            "include": {
              "type": "string",
              "description": "What to include: branches, tags, or commits",
              "enum": ["branches", "tags", "commits"],
              "default": "branches"
            }
          },
          "required": ["repo_id"]
        }
      },
      {
        "name": "run_hf_command",
        "description": "Execute ANY HuggingFace hf CLI command directly. Supports all hf subcommands: download, upload, space, endpoints, scan-cache, delete-cache, clear-cache, transfer, move, tag, env, whoami, list, jobs, api, login, logout. Automatically injects --json for known structured subcommands and auto-attaches the API token from settings. Output is auto-parsed as JSON when available. Use this as a universal CLI passthrough for operations not covered by dedicated tools.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "command": {
              "type": "string",
              "description": "hf subcommand to run (e.g. 'download', 'upload', 'space restart', 'endpoints list', 'scan-cache', 'transfer', 'move', 'tag list', 'env', 'whoami', 'list', 'api', 'jobs ps'). Do NOT include 'hf' prefix."
            },
            "args": {
              "type": "array",
              "items": { "type": "string" },
              "description": "Additional positional arguments (e.g. repo_id, file paths, space_id).",
              "default": []
            },
            "flags": {
              "type": "object",
              "description": "Additional flags as key-value pairs (e.g. {\"local-dir\": \"./models\", \"include\": \"*.safetensors\", \"type\": \"dataset\"}). Boolean flags use true as value.",
              "default": {}
            },
            "json_output": {
              "type": "boolean",
              "description": "Auto-add --json flag for structured output (recommended for agent parsing).",
              "default": true
            }
          },
          "required": ["command"]
        }
      },
      {
        "name": "hf_space_manage",
        "description": "Manage HuggingFace Spaces via hf CLI: list, restart, pause, resume, view logs, configure hardware, manage secrets, storage, env vars, and sleep time. Returns structured JSON output. Requires hf CLI and API token.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "action": {
              "type": "string",
              "description": "Space management action",
              "enum": ["list", "info", "restart", "pause", "resume", "logs", "hardware", "secrets", "storage", "env", "sleep-time", "duplicate"],
              "default": "list"
            },
            "space_id": {
              "type": "string",
              "description": "Space ID (e.g. 'username/my-space'). Required for all actions except 'list'."
            },
            "flags": {
              "type": "object",
              "description": "Additional flags (e.g. {\"limit\": 50, \"raw\": true} for logs; {\"flavor\": \"cpu-2x\"} for hardware).",
              "default": {}
            }
          }
        }
      },
      {
        "name": "hf_endpoints_manage",
        "description": "Manage HuggingFace Inference Endpoints via hf CLI: list, create, update, delete, pause, resume, scale. Supports all endpoint management operations. Returns structured JSON output. Requires hf CLI and API token.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "action": {
              "type": "string",
              "description": "Endpoint management action",
              "enum": ["list", "create", "update", "delete", "pause", "resume", "scale", "info", "list-jobs"],
              "default": "list"
            },
            "endpoint_id": {
              "type": "string",
              "description": "Endpoint ID (e.g. 'my-endpoint'). Required for all actions except 'list'."
            },
            "flags": {
              "type": "object",
              "description": "Additional flags for create/update/scale: {\"model\": \"meta-llama/Meta-Llama-3-8B\", \"revision\": \"main\", \"task\": \"text-generation\", \"accelerator\": \"cpu\", \"instance-type\": \"intel-cpu-xl-4\", \"instance-replicas\": 1, \"vendor\": \"aws\", \"region\": \"us-east-1\", \"namespace\": \"org-name\"}.",
              "default": {}
            }
          }
        }
      },
      {
        "name": "hf_cache_manage",
        "description": "Manage HuggingFace cache via hf CLI: scan cache to see all cached repos with sizes, show cache directory location, prune specific repos by type, or clear entire cache. Returns structured JSON with size summaries. Requires hf CLI.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "action": {
              "type": "string",
              "description": "Cache management action: scan (list all cached repos with sizes), dir (show cache directory), prune (delete specific cached repos), clear (clear entire cache).",
              "enum": ["scan", "dir", "prune", "clear"],
              "default": "scan"
            },
            "flags": {
              "type": "object",
              "description": "Additional flags. For prune: {\"repo\": \"repo-id-to-delete\"} or {\"type\": \"dataset\"}. For scan: {\"limit\": 50}.",
              "default": {}
            }
          }
        }
      },
      {
        "name": "hf_repo_transfer",
        "description": "Transfer or rename a HuggingFace repository between namespaces/users. Supports transfer (move repo to another user/org) and move (rename within same namespace). Requires a write-enabled API token. Returns structured output.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "action": {
              "type": "string",
              "description": "transfer: move repo to another user/org. move: rename within same namespace.",
              "enum": ["transfer", "move"],
              "default": "transfer"
            },
            "source_repo": {
              "type": "string",
              "description": "Source repository ID (e.g. 'my-username/old-repo-name')"
            },
            "target_repo": {
              "type": "string",
              "description": "Target repository ID. For transfer: new owner namespace (e.g. 'new-owner/repo-name'). For move: new name (e.g. 'my-username/new-repo-name')."
            },
            "repo_type": {
              "type": "string",
              "description": "Repository type",
              "enum": ["model", "dataset", "space"],
              "default": "model"
            },
            "flags": {
              "type": "object",
              "description": "Additional flags (e.g. {\"safe\": true} for dry-run).",
              "default": {}
            }
          },
          "required": ["source_repo"]
        }
      },
      {
        "name": "hf_tag_manage",
        "description": "Manage tags on a HuggingFace repository: list all tags, add a new tag, or remove a tag. Tags help categorize and discover repositories. Returns structured JSON output. Requires hf CLI and write-enabled API token.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "action": {
              "type": "string",
              "description": "Tag management action",
              "enum": ["list", "add", "remove"],
              "default": "list"
            },
            "repo_id": {
              "type": "string",
              "description": "Repository ID (e.g. 'username/my-repo')"
            },
            "tag": {
              "type": "string",
              "description": "Tag name to add or remove (e.g. 'transformers', 'pytorch', 'llama'). Required for add and remove actions."
            },
            "repo_type": {
              "type": "string",
              "description": "Repository type",
              "enum": ["model", "dataset", "space"],
              "default": "model"
            }
          },
          "required": ["repo_id"]
        }
      }
    ],
    "systemPrompt": "You are a HuggingFace Hub integration assistant with full CLI-level capabilities. You can search, discover, inspect, create, delete, manage, and administrate all HuggingFace Hub resources through both REST APIs and the hf CLI.\n\n## REST API Tools (no CLI required)\n- **search_models**: Find models by keyword, task, library, author, tags, language. Sort by popularity. Set full=true for complete metadata.\n- **get_model_info**: Get comprehensive model info: README, tags, downloads, config, library, and sibling files.\n- **search_datasets**: Find datasets by keyword, task, tags, language, or size category. Sort by 'created' for newest.\n- **get_dataset_info**: Get full dataset details including file tree and README.\n- **search_spaces**: Find interactive ML app demos on HuggingFace Spaces.\n- **get_trending**: Discover trending models, datasets, or Spaces from the last 24 hours or 7 days.\n- **list_collections**: Search user-curated collections of related models, datasets, and Spaces.\n- **list_repo_files**: List files in any repository. Explore contents, check file sizes, find specific files.\n- **list_repo_refs**: List branches, tags, or commits in a repository.\n- **get_repo_readme**: Fetch the README markdown of any repository.\n- **download_file**: Get a direct download URL or raw content of a specific file.\n- **run_inference**: Run a model on input text. Requires an API token.\n- **create_repo**: Create a new model, dataset, or Space repository. Supports description and license.\n- **upload_file**: Upload a text file to a repository.\n- **delete_file**: Delete a file from a repository.\n- **delete_repo**: Delete a repository. Irreversible.\n- **get_whoami**: Verify the configured API token and show current user info.\n\n## hf CLI Tools (require hf CLI installed)\n- **run_hf_command**: Universal CLI passthrough for any hf command not covered by dedicated tools.\n- **hf_space_manage**: Full Space management: list, restart, pause, resume, view logs, configure hardware, secrets, storage, env, sleep-time, duplicate.\n- **hf_endpoints_manage**: Inference Endpoints management: list, create, update, delete, pause, resume, scale, list-jobs.\n- **hf_cache_manage**: Cache management: scan (list cached repos with sizes), dir (show cache dir), prune (delete specific repos), clear (clear entire cache).\n- **hf_repo_transfer**: Transfer (move to another user/org) or rename repositories.\n- **hf_tag_manage**: List, add, or remove tags on repositories.\n\n## Guidelines\n- When a user asks about a task (e.g. 'I need a sentiment analysis model'), use search_models with the appropriate task and library filters first.\n- To find the latest datasets, use search_datasets with sort='created'.\n- To back up or inspect a dataset, first use get_dataset_info to see its structure, then use list_repo_files to explore subdirectories. For downloading single files, use download_file. For downloading entire repos or folders, use run_hf_command with 'download'.\n- For creating Spaces: set repo_type='space' and choose sdk='gradio' (default UI), 'streamlit', or 'static'. You can also provide an initial description that becomes the README.\n- Always present model/dataset results in a clear format: ID, type, downloads, likes, and brief description.\n- For inference and write operations, ensure the user has set their HuggingFace API token in plugin settings. If no token is configured, inform them how to get one at https://huggingface.co/settings/tokens.\n- Before destructive operations (delete_repo, delete_file), confirm the user's intent.\n- For CLI tools: prefer dedicated tools (hf_space_manage, hf_endpoints_manage, etc.) over run_hf_command when available. Use run_hf_command only for ad-hoc commands not covered by dedicated tools.\n- If hf CLI is not installed, guide the user to install it via 'pip install -U huggingface_hub' or 'curl -LsSf https://huggingface.co/cli/install.sh | bash'.\n- Popular model IDs for reference: meta-llama/Llama-3.1-8B-Instruct, mistralai/Mistral-7B-Instruct, bert-base-uncased, distilbert-base-uncased-finetuned-sst-2-english, google/flan-t5-large, sentence-transformers/all-MiniLM-L6-v2.\n- Respond in the language the user writes in."
  }
}
