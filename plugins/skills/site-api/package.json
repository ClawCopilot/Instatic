{
  "name": "@instatic/site-api",
  "version": "0.1.0",
  "description": "RESTful API skill that exposes site content, structure, and media for external agents and OpenClaw integration.",
  "instaticManifest": {
    "id": "instatic.site-api",
    "name": "Site API",
    "version": "0.1.0",
    "apiVersion": 1,
    "description": "Expose CMS site content, structure, and media through a clean RESTful API. Enables OpenClaw agents and other AI tools to read, create, and update site content programmatically.",
    "permissions": [
      "cms.routes.public"
    ],
    "entrypoints": {
      "server": "dist/index.js"
    },
    "resources": [],
    "adminPages": [],
    "settings": [
      {
        "id": "apiKey",
        "label": "API Access Key",
        "type": "password",
        "secret": true,
        "description": "Bearer token for API authentication. Leave empty for open access."
      },
      {
        "id": "rateLimit",
        "label": "Rate Limit (requests/min)",
        "type": "number",
        "min": 1,
        "max": 1000,
        "default": 60
      },
      {
        "id": "allowedOrigins",
        "label": "Allowed CORS Origins",
        "type": "textarea",
        "description": "One origin per line. Leave empty for all origins."
      },
      {
        "id": "readOnly",
        "label": "Read-Only Mode",
        "type": "toggle",
        "default": true
      }
    ]
  }
}
